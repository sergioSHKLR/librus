# Settings guide

Settings panel (library + reader entry) — planned PR 5.

| Area | Behavior |
|------|----------|
| Language | UI EN / PT |
| Theme | light / dark / system |
| Typography | font scale, line height |
| Links | density + providers (**wide only**) |
| Library data | Export / import JSON pack |
| App data | Export / import settings |
| Notes | **None** — Hypothes.is owns notes |
| Updates | Banner + force update (PWA) |
| Maintenance | Clear site data |

## Export pack (app only)

Includes settings, reading positions, density prefs. **Never** annotation bodies.
